CREATE DATABASE exemplo;
    use exemplo;
CREATE TABLE  produtos (
    id int(11) NOT NULL AUTO_INCREMENT,
    descricao varchar(50) DEFAULT NULL,
    PRIMARY KEY(id)
)

//Criação de uma base de dados de nome exemplo, contendo uma tabela de produtos com ID e descrição e chave primaria ID